import React from 'react';
import styled from 'styled-components';

const FooterNav = styled.footer`
    color: #ffffff;
    padding: 10px 20px;
    background: #000;
`;

const Footer = () => (
    <FooterNav>
        desarrollado for davamo
    </FooterNav>
);

export default Footer;